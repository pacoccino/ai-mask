{
    "name": "webai-ext-monorepo",
    "scripts": {
        "dev": "pnpm run --parallel dev"
    },
    "engines": {
        "node": ">=20",
        "pnpm": ">=8"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    }
}