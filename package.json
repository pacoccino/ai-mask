{
    "name": "webai-ext-monorepo",
    "scripts": {
        "build": "pnpm run --recursive build",
        "lint": "pnpm run --recursive lint",
        "dev": "pnpm run --parallel dev"
    },
    "engines": {
        "node": ">=20",
        "pnpm": ">=8"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    }
}